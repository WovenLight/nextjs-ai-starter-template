import { Inter } from "next/font/google";

export const inter = Inter({
    weight: ["400", "500", "600"],
    subsets: ["latin"],
    display: "swap",
    variable: "--font-inter",
});

export { Geist, Geist_Mono } from "next/font/google";
